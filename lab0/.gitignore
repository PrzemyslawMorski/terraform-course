# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
*.backup

# .tfstate lock info
*.tfstate.lock.info

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as password, private keys, and other secrets. 
*.tfvars

# Ignore override files as they are usually used to override resources locally and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include lockfile if necessary
.terraform.lock.hcl
